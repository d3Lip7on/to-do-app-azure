import { LoginModal } from '../features/login';
import { Page } from '../page/Page';

function App() {
	return (
		// <LoginModal
		// 	isOpen={true}
		// 	onClose={() => {
		// 		console.log('closed');
		// 	}}
		// 	onSubmit={() => {
		// 		console.log();
		// 	}}
		// />
		<Page />
	);
}

export default App;
